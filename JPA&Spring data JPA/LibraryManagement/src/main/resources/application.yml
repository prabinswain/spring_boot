spring:
  main:
    banner-mode: off
  application:
    name: LibraryManagement

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/jpa_db
    username: prabin
    password: Pkswain@123
#    driver-class-name: com.mysql.cj.jdbc.Driver # automatic detects


# logging
logging:
  level:
    root: info

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 7072