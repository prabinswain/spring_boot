<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>   <!-- POM file version-->
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.0</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.basic</groupId> <!--  unique is that needs to be exposed -->
	<artifactId>basic-projects-${release}</artifactId> <!--  my project name  -->
	<version>${revision}</version>  <!--  my project version name -->
	<name>basic-service</name>
    <packaging>jar</packaging>
	<description>This project I use for practice spring boot from basics again </description>
	<url/>
	<licenses>
		<license/>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag/>
		<url/>
	</scm>
	<properties> <!-- it is key value pairs we store in=t when we need to use something in whole POM the value of it -->
		<java.version>21</java.version>
        <release>sept2026</release>
        <revision>1.0.0</revision>
	</properties>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc</artifactId>
		</dependency>

        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <version>9.7.0</version>
        </dependency>

        <dependency>
            <groupId>org.hibernate</groupId>
            <artifactId>hibernate-core</artifactId>
            <version>6.5.2.Final</version>
            <type>pom</type>
            <scope>compile</scope>
        </dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc-test</artifactId>
			<scope>test</scope>
		</dependency>


	</dependencies>

    <!-- basically pom automatically downloads from .m2 from local and mvn central and in private repositories it is company based -->
<!--    <repositories>-->
<!--        <repository>-->
<!--            <id>API</id>-->
<!--            <url>https://pkgs.dev.azure.com/prabin-nexora</url>-->
<!--        </repository>-->
<!--    </repositories>-->


	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
		</plugins>
	</build>

</project>
